class Solution {
    int missingNum(int arr[]) {
        int n = arr.length + 1;
        int totalsum= n * (n + 1) / 2;
        int newsum= 0;
        for (int i = 0; i < arr.length; i++) {
            newsum=newsum+arr[i];
        }
        return totalsum-newsum;
    }
}
